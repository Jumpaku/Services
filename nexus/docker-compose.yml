version: '3'

services: 
  nexus:
    image: sonatype/nexus3
    container_name: nexus
    volumes: 
      - './nexus-data:/nexus-data'
    networks:
      - reverse_proxy_network
      - ldap_network

networks:
  reverse_proxy_network:
    external: true
  ldap_network:
    external: true